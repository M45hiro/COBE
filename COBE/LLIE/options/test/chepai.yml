### must be same as
name: test_chepai
suffix: ~  # add suffix to saved images
model: ll_dataset
distortion: sr
scale: 1
crop_border: ~  # crop border when evaluation. If None(~), crop the scale pixels
gpu_ids: [0]

datasets:
  test:  # the 1st test dataset
    name: test
    dataroot_GT: dataset/chepai/test/high
    dataroot_LQ: dataset/chepai/test/low

#### network structures
network_G:
  which_model_G: FourLLIE_Ycbcr_change
  # FourLLIE_Ycbcr_changessh
  nf: 64  # 64 #16 32 # 64

#### path
path:
  root: ./
  pretrain_model_G: save_new/chepai/experiments/train/models/83000_G.pth

