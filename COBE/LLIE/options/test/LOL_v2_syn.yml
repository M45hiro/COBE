### must be same as
name: test
suffix: ~  # add suffix to saved images
model: ll_dataset
distortion: sr
scale: 1
crop_border: ~  # crop border when evaluation. If None(~), crop the scale pixels
gpu_ids: [None]

datasets:
  test:  # the 1st test dataset
    name: test
    dataroot_GT: ../temp/original 
    dataroot_LQ: ../temp/original

#### network structures
network_G:
  which_model_G: FourLLIE_Ycbcr_change
  # FourLLIE_Ycbcr_changessh
  nf: 64  # 64 #16 32 # 64

#### path
path:
  root: 
  pretrain_model_G: LLIE/model_zoo/83000_G.pth

